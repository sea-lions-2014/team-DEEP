<ul>
  <% images_list.each do |image| %>
    <li id="thumb"><div id="txt"><%= image.captions.count %> Votes</div>
     <%= link_to image_tag(image.url), image_path(image), :class => "th" %></li>
  <% end %>
</ul>

