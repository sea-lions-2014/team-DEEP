<ul>
  <% images_list.each do |image| %>
    <li><p> <%= image.captions.count %> votes </p></li>
     <li><%= link_to image_tag("#{image.url}"), image_path(image), :class => "th" %></li>
  <% end %>
</ul>