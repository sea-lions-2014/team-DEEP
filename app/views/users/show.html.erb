<h1>Your Posts</h1>

<ul>
  <% if @images.present? %>
    <% @images.each do |image| %>
      <%= link_to image_tag("#{image.url}"), user_image_path(@user, image), :class => "th" %>
    <% end %>
  <% else %>
    <div class="imageerror">
    <p>No Images yet!</p>
  </div>
  <% end %>
</ul>

